server:
  port: 8089
spring:
  application:
    name: service-user
  data:
    mongodb:
      host: localhost
      port: 27017
      database: pass-genius
  logging:
    level:
      org.springframework: DEBUG

